# 🌍 智能旅游规划系统 (AI Travel Planner)

一个基于Spring Boot和Vue.js的智能旅游规划系统，集成阿里云百炼AI服务，为用户提供个性化的旅游行程规划建议。

## ✨ 主要特性

- 🤖 **AI智能规划** - 基于阿里云百炼大模型，提供智能旅游建议
- 👤 **用户管理** - 完整的用户注册、登录、权限管理
- 🗺️ **行程管理** - 创建、编辑、查看旅游行程
- 💰 **预算管理** - 行程预算规划和费用跟踪
- 📱 **响应式设计** - 支持PC和移动端访问
- 🔐 **安全认证** - JWT令牌认证，Spring Security保护
- 🐳 **Docker部署** - 支持容器化部署，一键启动

## 🏗️ 技术架构

### 后端技术栈
- **Spring Boot 3.5.7** - 主框架
- **Spring Security** - 安全框架
- **MyBatis** - ORM框架
- **MySQL 8.0** - 数据库
- **JWT** - 令牌认证
- **阿里云百炼SDK** - AI服务集成

### 前端技术栈
- **Vue.js 3** - 前端框架
- **Element Plus** - UI组件库
- **Axios** - HTTP客户端
- **HTML5/CSS3** - 页面结构和样式

## 🚀 快速开始

### 方式一：Docker部署（推荐）

#### 前置要求
- Docker Desktop
- docker-compose

#### 一键部署
```bash
# Windows用户
build-docker.bat

# Linux/Mac用户
chmod +x build-docker.sh
./build-docker.sh
```

#### 手动部署
```bash
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入阿里云百炼API密钥

# 2. 构建并启动
docker-compose up -d

# 3. 访问应用
# http://localhost:8080
```

### 方式二：本地开发部署

#### 前置要求
- Java 17+
- Maven 3.6+
- MySQL 8.0+

#### 快速启动
```bash
# Windows用户
start-local.bat

# Linux/Mac用户
chmod +x start-local.sh
./start-local.sh
```

#### 手动启动
```bash
# 1. 构建项目
mvn clean package -DskipTests

# 2. 启动应用
java -jar target/travel-ai-planner-0.0.1-SNAPSHOT.jar

# 3. 访问应用
# http://localhost:8080
```

## 📁 项目结构

```
├── src/main/java/com/travelai/          # Java源码
│   ├── config/                          # 配置类
│   ├── controller/                      # 控制器
│   ├── entity/                          # 实体类
│   ├── mapper/                          # MyBatis映射器
│   ├── service/                         # 服务层
│   └── util/                           # 工具类
├── src/main/resources/
│   ├── mapper/                          # MyBatis XML映射文件
│   ├── static/                          # 静态资源
│   ├── application.yml                  # 主配置文件
│   ├── application-docker.yml           # Docker环境配置
│   └── application-local.yml            # 本地环境配置
├── docker/                              # Docker相关文件
│   └── mysql/init.sql                   # 数据库初始化脚本
├── Dockerfile                           # Docker镜像构建文件
├── docker-compose.yml                   # 容器编排配置
├── build-docker.sh/bat                  # Docker构建脚本
├── start-local.sh/bat                   # 本地启动脚本
└── DOCKER.md                            # Docker部署详细文档
```

## 🔧 配置说明

### 环境变量配置

| 变量名 | 说明 | 必填 |
|--------|------|------|
| `DASHSCOPE_API_KEY` | 阿里云百炼API密钥 | ✅ |
| `MYSQL_ROOT_PASSWORD` | MySQL root密码 | ❌ |
| `MYSQL_DATABASE` | 数据库名称 | ❌ |
| `MYSQL_USER` | 数据库用户名 | ❌ |
| `MYSQL_PASSWORD` | 数据库密码 | ❌ |

### 数据库配置

系统会自动创建数据库表结构，包括：
- `users` - 用户表
- `itineraries` - 行程表
- `itinerary_details` - 行程详情表
- `attractions` - 景点表
- `user_favorites` - 用户收藏表

## 🌐 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 应用首页 | http://localhost:8080 | 主要访问入口 |
| 登录页面 | http://localhost:8080/static/login.html | 用户登录 |
| 注册页面 | http://localhost:8080/static/register.html | 用户注册 |
| 行程管理 | http://localhost:8080/static/trip-management.html | 行程列表 |
| 健康检查 | http://localhost:8080/actuator/health | 服务状态 |

## 📊 API接口文档

### 认证接口
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录
- `POST /api/auth/validate` - 令牌验证

### 行程接口
- `GET /api/itineraries` - 获取行程列表
- `POST /api/itineraries` - 创建行程
- `GET /api/itineraries/{id}` - 获取行程详情
- `PUT /api/itineraries/{id}` - 更新行程
- `DELETE /api/itineraries/{id}` - 删除行程

### AI接口
- `POST /api/ai/suggest` - 获取AI旅游建议
- `POST /api/ai/optimize` - 优化行程

## 🛠️ 开发指南

### 本地开发环境搭建

1. **克隆项目**
   ```bash
   git clone https://github.com/a2634787508-arch/ai-travel-planner.git
   cd ai-travel-planner
   ```

2. **配置数据库**
   ```sql
   CREATE DATABASE travel_ai CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

3. **配置应用**
   - 复制 `application-local.yml` 为 `application.yml`
   - 修改数据库连接配置
   - 配置阿里云百炼API密钥

4. **启动应用**
   ```bash
   mvn spring-boot:run
   ```

### 代码规范

- 使用Lombok简化代码
- 遵循RESTful API设计
- 统一异常处理
- 完善的日志记录

## 🔍 故障排除

### 常见问题

1. **Docker启动失败**
   - 检查Docker是否正在运行
   - 确认端口8080和3306未被占用
   - 查看日志：`docker-compose logs`

2. **数据库连接失败**
   - 检查数据库服务状态
   - 验证连接配置
   - 确认数据库用户权限

3. **AI功能不可用**
   - 检查阿里云百炼API密钥
   - 确认API密钥有效且有额度
   - 查看应用日志中的错误信息

4. **前端页面无法访问**
   - 检查静态资源配置
   - 确认Spring Boot已正确启动
   - 验证端口配置

## 📈 性能优化

### JVM调优
```bash
# 生产环境推荐参数
-Xmx1g -Xms512m -XX:+UseG1GC -XX:+UseContainerSupport
```

### 数据库优化
- 合理配置连接池
- 添加适当索引
- 定期清理日志

### 缓存策略
- Redis缓存热点数据
- 静态资源CDN加速
- 数据库查询优化

## 🔒 安全考虑

- JWT令牌过期机制
- 密码BCrypt加密
- SQL注入防护
- XSS攻击防护
- CSRF保护

## 📝 更新日志

### v1.0.0 (2024-01-15)
- ✨ 初始版本发布
- 🤖 集成阿里云百炼AI服务
- 👤 完整的用户管理系统
- 🗺️ 行程规划功能
- 🐳 Docker部署支持

## 🤝 贡献指南

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 📞 技术支持

- 📧 邮箱：support@travelai.com
- 🐛 问题反馈：[GitHub Issues](https://github.com/a2634787508-arch/ai-travel-planner/issues)
- 📖 文档：[DOCKER.md](DOCKER.md)

---

⭐ 如果这个项目对你有帮助，请给个星标支持一下！